# Production dependencies only — no GPU/training packages
# Use this for deployment: pip install -r requirements-prod.txt

# --- Agentic framework ------------------------------------------------------
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- Backend ----------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0
supabase>=2.4.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# --- Document parsing -------------------------------------------------------
pymupdf>=1.24.0

# --- Search -----------------------------------------------------------------
ddgs>=9.0.0
requests>=2.31.0
